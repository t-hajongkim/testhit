# 실습 안내

심평원 규정이 바뀌었을 때 **그 환자의 청구가 그대로 통과하는지**를, GitHub이 단계마다
확인해 주는 과정으로 직접 돌려 봅니다. 코드를 쓰지 않습니다. 환자 번호를 넣고, 읽고,
승인하면 됩니다.

---

## 이 실습에서 만드는 것

AI에게 "이 환자 청구 봐 줘"라고 한 번 물어보는 것과, 아래처럼 **단계마다 근거를 남기는
것**의 차이를 직접 보게 됩니다.

| 단계 | 무슨 일이 일어나나 | 내가 하는 일 |
|---|---|---|
| 1. 규정 | 심평원 공고에서 청구에 닿는 변경만 골라 PR로 올린다 | 읽고 머지 |
| 2. 요청 | 환자 번호 하나를 넣는다 | 입력 |
| 3. 조회 | 그 번호로 조회하고, 결과에서 번호를 뺀 뒤 AI에게 넘긴다 | (자동) |
| 4. 판단 | 진료일과 시행일을 대조해 청구 가능 여부를 가른다 | 보고서 읽기 |
| 5. 확인 | 저장소에 무엇이 남았는지 본다 | 확인 |

4단계가 핵심입니다. **같은 코드라도 진료일에 따라 답이 달라집니다.** 규정은 공고된
다음 날부터 자동으로 적용되지 않기 때문입니다.

---

## 미리 알아두실 것

- **AI가 한 번 판단하는 데 3~6분 걸립니다.** 화면이 멈춘 것처럼 보여도 정상입니다.
  기다리는 동안 다음 설명을 들으시면 됩니다.
- **환자 데이터는 가짜입니다.** 환자 50명 · 진료 50건의 합성 데이터입니다. 수가코드와
  규정만 실제입니다.
- **모든 번호에 진료내역이 있지는 않습니다.** 없는 번호를 넣으면 "진료내역이 없습니다"로
  끝납니다. 아래에 적힌 번호로 시작하세요.
- **AI는 데이터베이스에 접속하지 않습니다.** 조회는 워크플로가 끝내고, 그 결과에서 환자
  번호를 뺀 뒤에 넘깁니다. AI에게는 나이·성별·보험 구분과 진료 내용만 갑니다.

---

## 준비 (약 5분)

### 1. 내 저장소 만들기

**https://github.com/t-hajongkim/hira-billing-copilot** 로 들어가서 초록색
**Use this template** → **Create a new repository** 를 누릅니다.

- Repository name: 아무 이름, 단 **소문자로**
  (예: `hira-billing-실습` 말고 `hira-billing-practice`)
- **Public** 선택 (대시보드 주소가 이때만 만들어집니다. 데이터는 전부 합성입니다.)
- **Create repository** 클릭

> 소문자로 지어야 하는 이유: DB 이미지가 `ghcr.io/<아이디>/<저장소>-db` 로 올라가는데
> 컨테이너 저장소는 대문자를 받지 않습니다.

### 2. Actions 탭 열고 이미지가 만들어졌는지 보기

위쪽 **Actions** 탭을 누릅니다.
"I understand my workflows, go ahead and enable them" 버튼이 보이면 눌러 주세요.

**publish-db-image** 가 이미 돌고 있거나 끝나 있을 겁니다. 저장소를 만들 때 생기는
첫 커밋이 워크플로를 깨우기 때문에, **환자 데이터베이스는 저절로 만들어집니다.**
이어서 **build-dashboard** 가 돌면서 화면까지 구워집니다.

**build-dashboard** 이 두 번 도는 것이 정상입니다. 첫 번째는 이미지가 아직 없어서
그냥 넘어가고(초록불), 이미지가 올라온 뒤 두 번째가 실제로 화면을 굽습니다.

혹시 둘 다 없으면 **publish-db-image** → **Run workflow** 를 한 번 눌러 주세요.

### 3. AI가 풀 리퀘스트를 만들 수 있게 허용

**Settings** → 왼쪽 **Actions** → **General** → 맨 아래 *Workflow permissions* 에서

- **Allow GitHub Actions to create and approve pull requests** 체크
- **Save** 클릭

> 이걸 빠뜨리면 실습 4의 규정 동기화가 일은 다 해놓고 PR을 올리지 못합니다.

### 4. 대시보드 주소 켜기

**Settings** → 왼쪽 **Pages** → *Source* 를 **GitHub Actions** 로 바꿉니다.

> 이건 저장소 설정이라 워크플로가 대신 켤 수 없습니다. 안 켜도 실습은 다 됩니다 —
> 마지막에 화면을 웹으로 열 때만 필요합니다.

---

## 실습 1 — 규정이 무엇을 바꿨는지 읽기 (약 3분)

### 하는 일

저장소 첫 화면에서 **`rules/`** → **`HIRA_RULES.md`** 를 엽니다.

### 무엇을 보나

규정마다 이렇게 적혀 있습니다.

```markdown
## 치과 급여(100대100) 일부 코드 수가삭제
- 규정 ID: HIRA-2026-002
- 시행일: 2026-08-14
- 변경 유형: 코드 삭제
- 대상: AY102 · AY112
```

**공고일과 시행일이 다릅니다.** 이 파일이 이 실습의 전부입니다 — AI는 이 파일과
진료내역 두 가지만 놓고 판단합니다.

`## 걷기검사 신설` 항목도 함께 보세요. `F6051`·`F6052`·`F6053` 이 **2026-07-31 신설**
입니다. 신설이라는 말은 그 전에는 없던 코드라는 뜻입니다.

---

## 실습 2 — 규정에 걸리는 환자 (약 6분)

### 하는 일

**Actions** 탭 → 왼쪽 목록에서 **billing-intake** → 오른쪽 **Run workflow**

- 환자 ID: **`P00011`**
- 진료일: 비워 둡니다
- 판단에 쓸 모델: `haiku` (빠릅니다. `auto` 도 됩니다.)
- **Run workflow** 클릭

### 무슨 일이 일어나나

30초쯤 뒤 **billing-intake** 가 끝나고, 요약에 "진료비 판단을 시작했습니다" 가 뜹니다.
그 링크를 눌러 **진료비 청구 판단** 실행으로 갑니다. 3~6분 걸립니다.

그동안 워크플로는 이렇게 합니다.

```
P00011 로 조회  →  진료 3건  →  결과에서 환자 번호를 뺀다  →  AI 에게 넘긴다
```

### 결과 확인

판단 실행이 끝나면 그 페이지 아래 **Artifacts** 의 **`billing-report`** 를 내려받아
압축을 풀고 HTML을 엽니다.

**만 82세 · 여성 · 의료급여(2종)** 환자의 진료 3건이 이렇게 갈립니다.

| 진료일 | 항목 | 판단 |
|---|---|---|
| 2026-03-27 | CONTINUOUS & BOLUS PCA (`J4310055`) | 조건부 |
| 2026-08-12 | 코로나19 응급용 선별검사 (`D6583066`) | **급여 인정** |
| 2026-08-18 | 치과 급여(100대100) 항목 (`AY102`) | **불인정** |

마지막 줄이 이 실습의 그림입니다. `AY102` 는 **2026-08-14 자로 삭제된 코드**인데
진료일이 **2026-08-18** 입니다. 없어진 코드로 110,000원이 청구돼 있고, 그중
104,500원이 환자 본인부담으로 잡혀 있습니다.

보고서에는 판단마다 **적용 규정 · 시행일 · 진료일 비교**가 함께 적힙니다.
담당자가 확인할 수 없는 답은 쓸모가 없기 때문입니다.

---

## 실습 3 — 그대로 통과하는 환자 (약 6분)

### 하는 일

같은 자리에서 이번에는 환자 ID에 **`P00001`** 를 넣고 **Run workflow**.

### 결과 확인

같은 규정, 같은 기간인데 **전부 통과합니다.**

**만 22세 · 남성 · 의료급여1종 · 암 산정특례** 환자의 진료 3건입니다.

| 진료일 | 항목 | 판단 | 본인부담 |
|---|---|---|---|
| 2026-02-12 | CONTINUOUS & BOLUS PCA (`J4310055`) | **급여 인정** | 450원 (10%) |
| 2026-02-12 | 전파절삭기 (`M2041029`) | **급여 인정** | 9,800원 (10%) |
| 2026-05-19 | 코로나19 응급용 선별검사 (`D6583066`) | **급여 인정** | 8,500원 (10%) |

세 건 다 **기록된 규정에 닿는 코드가 아닙니다.** 걷기검사(`F605*`)도 아니고 삭제된 치과
코드(`AY102`)도 아니니, 규정이 아무리 바뀌어도 이 청구는 그대로입니다.

규정이 있다고 다 걸리는 것이 아닙니다. **그 환자의 그 진료일에 닿는 규정이 있을 때만**
걸립니다. 실습 2와 3의 차이가 그것뿐입니다.

> 시간이 남으면 **`P00013`** 도 넣어 보세요. 이번에는 반대 방향으로 걸립니다 —
> `F6051` 걷기검사를 **시행일(2026-07-31) 이전** 진료분에 청구했습니다.
> 그때는 없던 코드입니다.

---

## 실습 4 — 규정을 직접 받아 오기 (약 10분)

### 하는 일

**Actions** → **hira-rule-sync** → **Run workflow** (날짜는 비워 두셔도 됩니다)

### 무슨 일이 일어나나

파이썬이 심평원·복지부 공지사항을 받아 오고, **무엇이 우리 청구에 닿는지 고르는 판단만**
AI가 합니다. 닿는 변경이 있으면 `rules/HIRA_RULES.md` 에 **추가하는** PR을 엽니다.
기존 항목은 지우지 않습니다 — 2025-12-30 진료분은 그때의 규정으로 판단해야 하니까요.

**닿는 변경이 없어도 PR은 열립니다.** 그때는 규정이 아니라 "언제부터 언제까지 무엇을
보았고 왜 기록하지 않았는지"만 담깁니다. 확인했다는 사실도 남겨야 하기 때문입니다.

> 실제로 이렇게 나옵니다 — *확인 기간 2026-08-05 ~ 2026-08-21 · 전체 300건 · 후보 84건 ·
> 신규 공고 0건 · 기록한 규정 0건*

### 하는 일

**Pull requests** 탭에서 열린 PR을 읽습니다. 공고일·시행일·대상 코드가 제대로 적혔는지
보고, 맞으면 **Merge pull request**.

> **머지가 곧 승인입니다.** AI는 규정을 스스로 반영하지 않습니다. 사람이 머지한 규정만
> 다음 판단에 쓰입니다.

---

## 실습 5 — 무엇이 남았는지 확인 (약 3분)

판단 결과에는 그 환자의 진료 내용이 들어 있습니다. 그래서 저장소에 남기지 않습니다.

| 볼 곳 | 있어야 하는 것 |
|---|---|
| **Issues** 탭 | 0건 |
| **Pull requests** 탭 | 실습 4의 규정 PR 말고는 없음 |
| 판단 실행의 로그 | 환자 번호가 없음 (검색해 보세요) |
| 판단 실행의 Artifacts | `billing-report` — 7일 뒤 사라짐 |

환자 번호가 닿는 곳은 **billing-intake 실행의 입력값 한 곳뿐**이고, 그것도
**sweep-runs** 가 30분마다 돌며 지웁니다. `Actions` → `sweep-runs` 에서 확인할 수 있습니다.

---

## 화면으로 요청하기 (선택)

준비 4번에서 Pages를 켜셨다면 `https://<본인아이디>.github.io/<저장소이름>/` 이 열립니다.
검색창에 환자 번호를 넣고 Enter를 치면 Actions 폼을 거치지 않고 그 자리에서 실행되고,
진행과 결과까지 화면에 나옵니다.

한 번은 토큰을 넣어야 합니다 — 깃헙이 익명 요청으로는 워크플로를 시작해 주지 않기
때문입니다. 화면이 안내하는 대로 **토큰 만들기** → **Generate token** → 붙여넣기 하면
그 뒤로는 검색창만 씁니다. 토큰은 그 브라우저에만 저장됩니다.

---

## 잘 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| 워크플로가 빨간 X로 끝났다 (`write_package`) | 저장소 이름에 대문자가 있습니다. 소문자로 다시 만드세요. |
| "해당 환자 ID의 진료내역이 없습니다" | 그 번호에 진료가 없습니다. `P00011` · `P00013` 으로 해 보세요. |
| 판단 실행이 안 보인다 | intake 요약의 링크를 누르세요. 시작까지 30초쯤 걸립니다. |
| 5분째 노란 점이 돌고 있다 | 정상입니다. AI가 규정을 읽고 대조하는 중입니다. |
| 규정 PR 대신 아무 일도 없다 | 준비 3번을 빠뜨렸습니다. 설정하고 다시 실행하세요. |
| 대시보드 주소가 404다 | 준비 4번을 빠뜨렸거나 아직 배포 중입니다. Actions에서 build-dashboard 를 보세요. |
| 모델을 골랐더니 실패한다 | 요금제가 그 모델을 거절한 것입니다. `auto` 로 다시 실행하세요. |

---

## 오늘 가져가실 것

- **규정은 공고된 날부터 적용되지 않습니다.** 진료일과 시행일을 대조해야 답이 나오고,
  이 대조는 사람이 매번 하기에 너무 잦습니다.
- **AI에게 환자 식별정보를 주지 않고도** 청구 판단을 시킬 수 있습니다. 주고 나서 가리는
  것이 아니라, 조회 결과에서 애초에 빼면 됩니다.
- **머지가 곧 승인입니다.** 누가 언제 어떤 규정을 반영했는지 저장소에 남습니다.
- **판단 결과는 남기지 않는 편이 낫습니다.** 그 환자의 진료 내용이니까요.
- 이 구조는 청구만의 것이 아닙니다. 기준이 자주 바뀌고 확인이 필요한 업무라면 같은 틀을
  씁니다.

모든 판단은 청구 전 원무 담당자의 확인이 필요합니다.
